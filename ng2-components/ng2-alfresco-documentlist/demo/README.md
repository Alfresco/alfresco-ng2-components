# DocumentList demo

Install:

```
npm install
```

Set authentication token value in the `index.html`

```html
<script>
    localStorage.setItem('token', '<AUTH-TOKEN>');
</script>
```

Run the project:

```
npm start
```