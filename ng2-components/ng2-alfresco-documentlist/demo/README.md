# DocumentList demo

Install:

```
npm install
```

Update host and credentials

**src/main.ts**
```ts
login() {
    let host = 'http://myalfrescoip';
    let credentials = { "userId": "admin", "password": "admin" };
    ...
}
```

Run the project:

```
npm start
```