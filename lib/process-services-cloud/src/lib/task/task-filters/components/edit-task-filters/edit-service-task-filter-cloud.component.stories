import { moduleMetadata, Story, Meta } from '@storybook/angular';
import { EditServiceTaskFilterCloudComponent } from './edit-service-task-filter-cloud.component';

export default {
  title: 'EditServiceTaskFilterCloudComponent',
  component: EditServiceTaskFilterCloudComponent,
  decorators: [
    moduleMetadata({
      imports: [],
    })
  ],
} as Meta<EditServiceTaskFilterCloudComponent>;

const Template: Story<EditServiceTaskFilterCloudComponent> = (args: EditServiceTaskFilterCloudComponent) => ({
  component: EditServiceTaskFilterCloudComponent,
  props: args,
});


export const Primary = Template.bind({});
Primary.args = {
}