    syntax:
      return:
        type: >-
          <%- child['type'] ? include("type", {type: child.type}).trim() : '' %>
        defaultValue: >-
          <%- (child.defaultValue || "").length < 20 ? child.defaultValue : "" %>
